---
- hosts: ldapserver
  become: yes
  tasks:
  - name: install packages
    yum: 
      name: ipa-server
      state: present
      update_cache: true
      
- hosts: ldapclient
  become: yes
  tasks:
  - name: install packages
    yum: 
      name: freeipa-client
      state: present    
      update_cache: true
